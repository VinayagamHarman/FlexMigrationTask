/**
 * Generated by Apache Royale Compiler from spark/core/NavigationUnit.as
 * spark.core.NavigationUnit
 *
 * @fileoverview
 *
 * @suppress {missingRequire|checkTypes|accessControls}
 */

goog.provide('spark.core.NavigationUnit');
/* Royale Dependency List: org.apache.royale.events.KeyboardEvent,XML*/




/**
 * @constructor
 */
spark.core.NavigationUnit = function() {
};


/**
 * Prevent renaming of class. Needed for reflection.
 */
goog.exportSymbol('spark.core.NavigationUnit', spark.core.NavigationUnit);


spark.core.NavigationUnit.get__HOME = function() {
  var value = org.apache.royale.events.KeyboardEvent.KEYCODE_HOME;
  Object.defineProperty(spark.core.NavigationUnit, 'HOME', { value: value, writable: false });
  return value;
};
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
spark.core.NavigationUnit.HOME;

Object.defineProperties(spark.core.NavigationUnit, /** @lends {spark.core.NavigationUnit} */ {
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
HOME: {
  get: spark.core.NavigationUnit.get__HOME,
  configurable: true}});


spark.core.NavigationUnit.get__END = function() {
  var value = org.apache.royale.events.KeyboardEvent.KEYCODE_END;
  Object.defineProperty(spark.core.NavigationUnit, 'END', { value: value, writable: false });
  return value;
};
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
spark.core.NavigationUnit.END;

Object.defineProperties(spark.core.NavigationUnit, /** @lends {spark.core.NavigationUnit} */ {
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
END: {
  get: spark.core.NavigationUnit.get__END,
  configurable: true}});


spark.core.NavigationUnit.get__UP = function() {
  var value = org.apache.royale.events.KeyboardEvent.KEYCODE_UP;
  Object.defineProperty(spark.core.NavigationUnit, 'UP', { value: value, writable: false });
  return value;
};
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
spark.core.NavigationUnit.UP;

Object.defineProperties(spark.core.NavigationUnit, /** @lends {spark.core.NavigationUnit} */ {
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
UP: {
  get: spark.core.NavigationUnit.get__UP,
  configurable: true}});


spark.core.NavigationUnit.get__DOWN = function() {
  var value = org.apache.royale.events.KeyboardEvent.KEYCODE_DOWN;
  Object.defineProperty(spark.core.NavigationUnit, 'DOWN', { value: value, writable: false });
  return value;
};
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
spark.core.NavigationUnit.DOWN;

Object.defineProperties(spark.core.NavigationUnit, /** @lends {spark.core.NavigationUnit} */ {
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
DOWN: {
  get: spark.core.NavigationUnit.get__DOWN,
  configurable: true}});


spark.core.NavigationUnit.get__LEFT = function() {
  var value = org.apache.royale.events.KeyboardEvent.KEYCODE_LEFT;
  Object.defineProperty(spark.core.NavigationUnit, 'LEFT', { value: value, writable: false });
  return value;
};
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
spark.core.NavigationUnit.LEFT;

Object.defineProperties(spark.core.NavigationUnit, /** @lends {spark.core.NavigationUnit} */ {
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
LEFT: {
  get: spark.core.NavigationUnit.get__LEFT,
  configurable: true}});


spark.core.NavigationUnit.get__RIGHT = function() {
  var value = org.apache.royale.events.KeyboardEvent.KEYCODE_RIGHT;
  Object.defineProperty(spark.core.NavigationUnit, 'RIGHT', { value: value, writable: false });
  return value;
};
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
spark.core.NavigationUnit.RIGHT;

Object.defineProperties(spark.core.NavigationUnit, /** @lends {spark.core.NavigationUnit} */ {
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
RIGHT: {
  get: spark.core.NavigationUnit.get__RIGHT,
  configurable: true}});


spark.core.NavigationUnit.get__PAGE_UP = function() {
  var value = org.apache.royale.events.KeyboardEvent.KEYCODE_PAGEUP;
  Object.defineProperty(spark.core.NavigationUnit, 'PAGE_UP', { value: value, writable: false });
  return value;
};
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
spark.core.NavigationUnit.PAGE_UP;

Object.defineProperties(spark.core.NavigationUnit, /** @lends {spark.core.NavigationUnit} */ {
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
PAGE_UP: {
  get: spark.core.NavigationUnit.get__PAGE_UP,
  configurable: true}});


spark.core.NavigationUnit.get__PAGE_DOWN = function() {
  var value = org.apache.royale.events.KeyboardEvent.KEYCODE_PAGEDOWN;
  Object.defineProperty(spark.core.NavigationUnit, 'PAGE_DOWN', { value: value, writable: false });
  return value;
};
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
spark.core.NavigationUnit.PAGE_DOWN;

Object.defineProperties(spark.core.NavigationUnit, /** @lends {spark.core.NavigationUnit} */ {
/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
PAGE_DOWN: {
  get: spark.core.NavigationUnit.get__PAGE_DOWN,
  configurable: true}});


/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
spark.core.NavigationUnit.PAGE_LEFT = 0x2397;


/**
 * @export
 * @nocollapse
 * @const
 * @type {number}
 */
spark.core.NavigationUnit.PAGE_RIGHT = 0x2398;


/**
 *  Returns <code>true</code> if the <code>keyCode</code> maps directly 
 *  to a NavigationUnit enum value.
 *
 *  @asparam keyCode A key code value. 
 *
 *  @asreturn <code>true</code> if the <code>keyCode</code> maps directly 
 *  to a NavigationUnit enum value.
 * 
 *  @langversion 3.0
 *  @playerversion Flash 10
 *  @playerversion AIR 1.5
 *  @productversion Flex 4
 * @export
 * @param {number} keyCode
 * @return {boolean}
 */
spark.core.NavigationUnit.isNavigationUnit = function(keyCode) {
  switch (keyCode) {
    case org.apache.royale.events.KeyboardEvent.KEYCODE_LEFT:
      return true;
    case org.apache.royale.events.KeyboardEvent.KEYCODE_RIGHT:
      return true;
    case org.apache.royale.events.KeyboardEvent.KEYCODE_UP:
      return true;
    case org.apache.royale.events.KeyboardEvent.KEYCODE_DOWN:
      return true;
    case org.apache.royale.events.KeyboardEvent.KEYCODE_PAGEUP:
      return true;
    case org.apache.royale.events.KeyboardEvent.KEYCODE_PAGEDOWN:
      return true;
    case org.apache.royale.events.KeyboardEvent.KEYCODE_HOME:
      return true;
    case org.apache.royale.events.KeyboardEvent.KEYCODE_END:
      return true;
    default:
      return false;
  }
};


/**
 * Metadata
 *
 * @type {Object.<string, Array.<Object>>}
 */
spark.core.NavigationUnit.prototype.ROYALE_CLASS_INFO = { names: [{ name: 'NavigationUnit', qName: 'spark.core.NavigationUnit', kind: 'class' }] };



/**
 * Reflection
 *
 * @return {Object.<string, Function>}
 */
spark.core.NavigationUnit.prototype.ROYALE_REFLECTION_INFO = function () {
  return {
    methods: function () {
      return {
        '|isNavigationUnit': { type: 'Boolean', declaredBy: 'spark.core.NavigationUnit', parameters: function () { return [ 'uint', false ]; }}
      };
    }
  };
};
/**
 * @const
 * @type {number}
 */
spark.core.NavigationUnit.prototype.ROYALE_COMPILE_FLAGS = 10;
